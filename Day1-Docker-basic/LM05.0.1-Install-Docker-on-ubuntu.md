These steps work for **Ubuntu 20.04 / 22.04 / 24.04**.

---

## 1️⃣ Remove old Docker versions (if any)

```bash
sudo apt-get remove docker docker-engine docker.io containerd runc
```

---

## 2️⃣ Update packages

```bash
sudo apt update
sudo apt install -y ca-certificates curl gnupg
```

---

## 3️⃣ Add Docker’s official GPG key

```bash
sudo install -m 0755 -d /etc/apt/keyrings
curl -fsSL https://download.docker.com/linux/ubuntu/gpg | sudo gpg --dearmor -o /etc/apt/keyrings/docker.gpg
sudo chmod a+r /etc/apt/keyrings/docker.gpg
```

---

## 4️⃣ Add the Docker repository

```bash
echo \
  "deb [arch=$(dpkg --print-architecture) signed-by=/etc/apt/keyrings/docker.gpg] https://download.docker.com/linux/ubuntu \
  $(. /etc/os-release && echo "$VERSION_CODENAME") stable" | \
  sudo tee /etc/apt/sources.list.d/docker.list > /dev/null
```

---

## 5️⃣ Install Docker Engine

```bash
sudo apt update
sudo apt install -y docker-ce docker-ce-cli containerd.io docker-buildx-plugin docker-compose-plugin
```

---

## 6️⃣ Start and enable Docker

```bash
sudo systemctl start docker
sudo systemctl enable docker
```

Check status:

```bash
sudo systemctl status docker
```

---

## 7️⃣ Run Docker without sudo (recommended)

```bash
sudo usermod -aG docker $USER
```

👉 **Log out and log back in** (or reboot) for this to take effect.

---

## 8️⃣ Test Docker

```bash
docker run hello-world
```

If everything is correct, you’ll see a message from **Docker** saying installation is working 🎉

---

### 🔧 Optional: Check versions

```bash
docker --version
docker compose version
```

