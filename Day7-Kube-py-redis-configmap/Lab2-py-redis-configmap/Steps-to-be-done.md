* download this code keep it in a single folder
* Create an GitHub repo 
* clone the the repo
* move all the files to the local repo 
* push it to GitHub
* create an Jenkins pipeline using the same jenkinsfiles that was used in previous class with the trigger option
* build the job to deploy on the minikube

